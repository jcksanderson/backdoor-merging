merge_method: task_arithmetic

base_model: ./llama

models:
  - model: placeholder
    parameters:
      weight: 0.5

  - model: placeholder
    parameters:
      weight: 0.5

dtype: float32

tokenizer_source: base
